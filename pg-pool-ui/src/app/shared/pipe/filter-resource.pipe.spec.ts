import { FilterResourcePipe } from './filter-resource.pipe';

describe('FilterResourcePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterResourcePipe();
    expect(pipe).toBeTruthy();
  });
});
